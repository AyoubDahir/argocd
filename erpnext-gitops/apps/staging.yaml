apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: erpnext-staging
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/AyoubDahir/argocd.git'
    targetRevision: HEAD
    path: erpnext-gitops/environments/staging
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: erpnext-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
